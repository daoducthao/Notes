<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:Notes.Views"
       x:Class="Notes.AppShell">
    <!-- Display a bottom tab bar containing two tabs -->
    <TabBar>
        <ShellContent Title="Ipay"
                      Icon="apple.png"
                      ContentTemplate="{DataTemplate views:NotesPage}" />
        <ShellContent Title="Ưu đãi"
                      Icon="discount.png"
                      ContentTemplate="{DataTemplate views:AboutPage}" />
        <ShellContent Title="Lịch sử GD"
                      Icon="history.png"
                      ContentTemplate="{DataTemplate views:History}" />
        <ShellContent Title="Chat"
                      Icon="comment.png"
                      ContentTemplate="{DataTemplate views:Chat}" />
        <ShellContent Title="Ví của tôi"
                      Icon="money.png"
                      ContentTemplate="{DataTemplate views:Wallet}" />
    </TabBar>
</Shell>